%h1
  会員名簿

- if @members.present?
  %table.list
    %tr
      %th
        背番号
      %th
        ユーザー名
      %th
        氏名
      %th
        操作

    - @members.each do |member|
      %tr
        %td{ style: "text-align: right"}
          = member.number
        %td
          = member.name
        %td
          = member.full_name
        %td
- else
  %p
    会員情報がありません